## Scope

This repository focuses on alignment stability at the pipeline level.

It does not propose new training methods.

It does not modify model weights.

Instead, it explores whether alignment reliability can improve
by separating reasoning, policy enforcement, and auditing
into distinct authority layers.